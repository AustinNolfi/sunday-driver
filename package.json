{
  "name": "react-express",
  "version": "1.0.0",
  "description": "Combines express back end web server and react front end",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "server": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Brad Traversy",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^6.0.0",
    "express": "^4.18.2",
    "git": "^0.1.5"
  }
}
